---
title: "Social media footprint"
description: "An AI-powered search supported by human review, used to detect negative behaviours such as bullying, racism, extremist views, and nudity on disclosed social media accounts."
---

<Tabs>
  <Tab title="GraphQL">
    ```
    curl -X POST \
         -H "Content-Type: application/json" \
         -H "Authorization: Bearer YOUR_PUBLIC_API_TOKEN" \
         -d '{
           "query": "query FetchCheckRequest {
              fetchCheckRequest(where: { externalId: \"SCEXAMPLE\" }) {
                socialMediaFindings {
                  aiSentimentAnalisys
                  list {
                    type
                    uri
                    remarks
                    tags
                    image_url
                  }
                }
              }
           }"
         }' \
         https://api.sourcecompliant.com/applicant
    
    ```
  </Tab>
  <Tab title="cURL">
    ```bash
    curl -X POST \
         -H "Content-Type: application/json" \
         -H "Authorization: Bearer YOUR_PUBLIC_API_TOKEN" \
         -d '{
           "query": "query FetchCheckRequest {
              fetchCheckRequest(where: { externalId: \"SCEXAMPLE\" }) {
                socialMediaFindings {
                  aiSentimentAnalisys
                  list {
                    type
                    uri
                    remarks
                    tags
                    image_url
                  }
                }
              }
           }"
         }' \
         https://api.sourcecompliant.com/applicant
    
    ```
  </Tab>
  <Tab title="Javascript">
    ```javascript
    const fetchSocialMediaFindings = async () => {
      try {
        const response = await fetch("https://api.sourcecompliant.com/applicant", {
          method: "POST",
          headers: {
            "Content-Type": "application/json",
            "Authorization": "Bearer YOUR_PUBLIC_API_TOKEN",
          },
          body: JSON.stringify({
            query: `
              query FetchCheckRequest {
                fetchCheckRequest(where: { externalId: "SCEXAMPLE" }) {
                  socialMediaFindings {
                    aiSentimentAnalisys
                    list {
                      type
                      uri
                      remarks
                      tags
                      image_url
                    }
                  }
                }
              }
            `,
          }),
        });
    
        if (!response.ok) {
          throw new Error(`HTTP error! status: ${response.status}`);
        }
    
        const data = await response.json();
        console.log("Response data:", data);
      } catch (error) {
        console.error("Error:", error);
      }
    };
    
    // Call the function
    fetchSocialMediaFindings();
    
    ```
  </Tab>
</Tabs>